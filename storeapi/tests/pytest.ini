# pytest.ini
[pytest]
# Set the test directory
testpaths = tests

# Optional: specify patterns for test files and functions
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Set the rootdir if needed (this can help with test discovery)
rootdir = .
